#include<stdio.h>
void average (int gn,int arr[20])
{
  double sum,avg;
  int i,c=0;
  for(i=0;i<n,i++)
    {
     if(arr[i]%2!=0)
     {
    sum=sum+arr[i];
    c++;
       }
    }
}

int main()
{
  int n,arr[20],i;
  printf("Enter the number of elements\n");
  scanf("%d",&n);
  printf("Enter the elements\n");
  for(i=0;i< n;i++);
  scanf("%d",&arr[i]);
  average(n,arr);
  return 0;
}